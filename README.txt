El uso de grupos nos permite definir la estructura una sola vez y reutilizarla. En este proyecto:

Sin grupos: Habríamos tenido que repetir la definición de los 4 elementos de hardware (cpu, ram, gpu, discos) y los 3 atributos del servidor (id, rack, estado) cada vez que definiéramos un tipo de nodo o servidor diferente.

¿Qué error os da VS Code si intentáis poner dos servidores con el mismo ID?
Identity constraint validation error: field 'id' has a duplicate value